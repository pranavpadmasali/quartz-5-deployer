.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix

# Quartz plugins are re-installed on each build (locally and in CI) from
# quartz.lock.json, so they don't need to be committed.
.quartz/
quartz/cli/tui/dist/
